import 'package:flutter/material.dart';

class AppColors {
  static const approve = Color(0xFF00897B);      // Teal[600]
  static const reject = Color(0xFFD32F2F);       // Red[700]
  static const deactivate = Color(0xFFFFB300);   // Amber[700]
  static const activate = Color(0xFF388E3C);     // LightGreen[700]
  static const edit = Color(0xFF0097A7);         // Cyan[700]
  static const delete = Color(0xFFC2185B);       // Pink[800]
}